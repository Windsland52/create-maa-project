[project]
name = "create-maa-project"
version = "0.1.0"
description = "用于创建和维护 MaaFW 应用项目的脚手架 CLI / Scaffold and maintenance CLI for MaaFW application projects"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/Windsland52/create-maa-project"
Source = "https://github.com/Windsland52/create-maa-project"

[project.scripts]
create-maa-project = "create_maa_project.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["py-wrapper/create_maa_project"]

