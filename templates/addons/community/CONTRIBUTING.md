# Contributing to {{displayName}}

## Development

Run the local checks before sending changes:

```bash
pnpm check
```

Keep MaaFW resource paths using forward slashes and keep resource packs ordered in `maa-project.json`.
