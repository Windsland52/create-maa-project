{
    "devDependencies": {{devDependencies}},
    "engines": {
        "node": "{{nodeRange}}"
    },
    "license": "{{license}}",
    "name": "{{name}}",
    "packageManager": "pnpm@{{pnpmVersion}}",
    "private": true,
    "scripts": {{scripts}},
    "type": "module",
    "version": "{{version}}"
}
